<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opay.dao.AccountDao">

    <resultMap id="accountResultMap" type="com.opay.entity.AccountDo">
        <result column="id" property="id"></result>
        <result column="name" property="name"></result>
        <result column="id_card" property="idCard"></result>
        <result column="nick_name" property="nickName"></result>
        <result column="mobile_number" property="mobileNumber"></result>
        <result column="balance" property="balance"></result>
        <result column="gmt_create" property="gmtCreate"></result>
        <result column="gmt_modified" property="gmtModified"></result>
    </resultMap>

    <insert id="save" parameterType="com.opay.entity.AccountDo">
        insert into account (name,id_card,nick_name,mobile_number,balance)
        values (#{name},#{idCard},#{nickName},#{mobileNumber},#{balance})
    </insert>

    <update id="updateById" parameterType="com.opay.entity.AccountDo">
        update account
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="idCard != null">
                id_card = #{idCard},
            </if>
            <if test="nickName != null">
                nick_name = #{nickName},
            </if>
            <if test="mobileNumber != null">
                mobile_number = #{mobileNumber},
            </if>
            <if test="balance != null">
                balance = #{balance}
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="listAccountByIdOrIdCardOrName" parameterType="com.opay.entity.AccountDo" resultMap="accountResultMap">
        SELECT
            ac.id,
            ac.`name`,
            ac.id_card,
            ac.mobile_number,
            ac.balance,
            ac.nick_name
        FROM
            account ac
        <where>
            <if test="id != null">
                ac.id = #{id}
            </if>
            <if test="idCard != null">
                and ac.id_card = #{idCard}
            </if>
            <if test="name != null">
                AND ac.`name` = #{name}
            </if>
        </where>
    </select>
</mapper>